name: Add time log entry

on:
  issues:
    types: labeled

jobs:
  add-time-log-entry:
    runs-on: ubuntu-latest
    steps:
      - name: Add time log entry to issue body
        uses: actions/github-script@v7
        with:
          script: |
            Object.keys(github).map(console.log)
            const issueNumber = github.event.issue.number;

            const now = new Date();
            const nowString = now.toISOString().replace("T", " ").slice(0, -5);

            const newTimeLogEntry = `| ${nowString} |                     |          |`;
            const newIssueBody = github.event.issue.body.replace(
              /(?<=## Time log.+\|)\n(?!\|)/ms,
              "$&" + newTimeLogEntry + "\n"
            );

            await github.rest.issues.update({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: issueNumber,
              body: newIssueBody
            });
